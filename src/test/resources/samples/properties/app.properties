app.name=polyconf
app.version=1.0.0
app.description=A polyglot configuration parser

server.host=0.0.0.0
server.port=8080
server.debug=false

database.host=localhost
database.port=5432
database.name=myapp
database.pool.size=10

features.auth.enabled=true
features.logging.enabled=true
features.metrics.enabled=true
features.timeout.ms=30000
